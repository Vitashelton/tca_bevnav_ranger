# Core training stack. Versions are lower bounds known to work with the
# exported ONNX opset 17 and TensorRT 8.x on Jetson Orin Nano (JetPack 5/6).
torch>=2.0
numpy>=1.23
pyyaml>=6.0
# Optional but recommended:
onnx>=1.14
onnxruntime>=1.15        # for validate_policy.py (CPU is fine)
tensorboard>=2.13        # logger falls back to CSV if absent
# rosbag2_py / rclpy come from your ROS2 Humble install, NOT pip.
